/**
 * Button Component
 *
 * This re-exports the Kapwa design system Button component.
 * The Button supports variants: primary, secondary, outline, ghost, link
 * and sizes: sm, md, lg. It also includes loading states and icon support.
 *
 * @example
 * ```tsx
 * <Button variant="primary" size="md" leftIcon={<Icon />}>
 *   Click me
 * </Button>
 * ```
 */

export { Button } from '@/kapwa/button';
export { Button as default } from '@/kapwa/button';
